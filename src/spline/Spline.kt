package spline

class Spline(start: Double) {
    // generate an interpolated path from the Spline points
    fun interpolate(): Sequence<Pair<Double, Double>> {
        return TODO()
    }

    // return a new Spline a bit different from the current one
    fun mutate(): Spline {
        return TODO()
    }
}